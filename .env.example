GEMINI_API_KEY=
GEMINI_LLM_MODEL=gemini-3.1-flash-lite
GEMINI_EMBEDDING_MODEL=models/embedding-001
# Optional fixed embedding dimension. Leave empty to auto-detect from the
# first real Gemini response (the vec0 index is created with that dimension).
EMBEDDING_DIMENSION=